import {expect} from 'chai'
import documentService from '../../src/serivces/document'

describe('setSlideTitle action creator', () => {

    describe('when called for "current slide"', () => {

        it('returns a thunk', () => {

        })

        it('dispaches an action for the slides', () => {

        })

        it('dispatches an action for current slide', () => {

        })
    })

    describe('when called for non-current slide', () => {

        it('returns a thunk', () => {

        })

        it('dispaches an action for the slides', () => {

        })

        it('does not dispatch an action for current slide', () => {

        })
    })

})